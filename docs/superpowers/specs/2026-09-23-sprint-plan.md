# Спринт с 23.09.2026: план читается без вопросов

Принят владельцем 23.09.2026. Точка входа для сессий этого спринта. Решения по способу работы — в ретро шага А (`2026-09-16-step-a-retro.md`, раздел 9), здесь не повторяются.

---

## 1. Где мы на маршруте

```
а) чиним критичное                ← закрыт по взятому списку: 10 PR, 18 задач
б) фиксируем состояние,           ← первая половина закрыта, вторая — с этого спринта
   доводим до пригодного к показу
в) многопользовательская часть    ← ждёт решения ANO-143
г) выкладываем на сервер
д) всё остальное
```

Ретро пометило «б» закрытым, но по дороге формулировка сузилась. В исходной (`2026-09-11-two-prompts.md:47`) у «б» две половины. Первая сделана: состояние зафиксировано — ручной проход ANO-50, карта бэклога, ноль открытых PR. Вторая, «довести до пригодного к показу», не начиналась: из 49 задач, которые карта бэклога отнесла к «б», закрыты две (ANO-119, ANO-122).

Владелец 23.09: «согласен с предложением им и заняться, довести его до конца».

**Почему не «в» сразу.** «В» не может стартовать без решения о способе входа (ANO-143 блокирует весь эпик). Список категорий станет шаблоном каждого нового пользователя (ANO-151) — сначала список, потом шаблон. Миграция «в» (владелец в 11 таблиц) — самая рискованная в проекте, и CI с прогоном миграций должен появиться раньше неё.

## 2. Цель

Видно, какие траты точные, а какие прикидка, и план читается без вопросов. CI проверяет каждый PR целиком и не тратит на это токены сессии.

## 3. Задачи — порядок по зависимостям, а не по важности

| # | задача | зачем именно сейчас |
|---|---|---|
| 0 | **Гигиена** — сделано 23.09 | дампы с реальными деньгами в корне не ловились `.gitignore` (добавлен `/*.sql`); ANO-162…165 получили проект; заголовок ANO-173 противоречил решению 23.09 — переписан; заведён дефект ANO-178 |
| 1 | [ANO-26](https://linear.app/anotherspace1/issue/ANO-26) CI на PR | в полном объёме по 9.3 ретро: фронт, `verify`, оба образа, миграции на небоевом дампе. В спринте две миграции, следом — самая рискованная. Полный прогон переезжает из сессии в CI |
| 2 | [ANO-175](https://linear.app/anotherspace1/issue/ANO-175) сторож словаря | правила 1, 2, 5, 12, 13 одним тестом по строкам интерфейса. Встаёт раньше, чем пункты 3–4 напишут новые тексты. Нынешние нарушения — в список исключений, который может только сокращаться |
| 3 | [ANO-173](https://linear.app/anotherspace1/issue/ANO-173) твёрдость: имена и определение | корень остального. Расчёт не трогаем (решение 23.09); Обязательство / Ожидание / Хотелка на экране, определение — в правила продукта. Внутри развилка «одно поле или два» с ANO-53 — постановкой владельцу |
| 4 | [ANO-176](https://linear.app/anotherspace1/issue/ANO-176) журнал: два списка, закрытие одной кнопкой | самое заметное в ежедневной работе. Первым шагом спека с макетом: ипотека на 25 лет — 298 строк (ANO-130), в «точном» списке они задавят всё |
| 5 | [ANO-177](https://linear.app/anotherspace1/issue/ANO-177) целостность идеи | документ без кода, до текстов метода на экране. Повод: по памяти, Simplifi и PocketGuard уже вычитают взносы в цели до «свободно» |
| 6 | [ANO-143](https://linear.app/anotherspace1/issue/ANO-143) способ входа | решение владельца, кода нет. Постановка — отдельным сообщением ближе к концу спринта, чтобы «в» стартовал без простоя |

**Резерв:** ANO-172 (наша регрессия из PR #50: план-факт на «Аналитике» нечитаем), ANO-163 (привязка копилки переписывает капитал прошлых месяцев). Обе High.

**Категории исключены из спринта** 23.09: владелец ведёт их в отдельном чате под ANO-151. Дефект ANO-178 (пустой выбор категории в форме правки) заведён здесь, потому что проявится именно при смене набора, — связан с ANO-151.

## 4. Сознательно не берём

* код шага «в» — ждёт пункта 6;
* дизайн-сессию по экранам ANO-128 — см. раздел 5, это главный кусок остатка «б»;
* пролонгацию по умолчанию и цепочку как объект (требования 2.2, 5.2) — одно решение про кнопку «повторять», после журнала;
* объяснение метода целиком (6.1) — после ANO-177;
* ANO-169, 171, 174 — на паузе до повторного воспроизведения.

## 5. Что остаётся в «б» после спринта

Вопрос владельца 23.09: «это весь „б“ или ещё что-то есть?» — **не весь.** Спринт закрывает две задачи из сорока семи открытых (ANO-26, ANO-129 вместе с ней), наполовину — ANO-63 (ревизию экранов берёт сторож) и задевает ещё три: ANO-42 (разделение списка), ANO-123 и ANO-124 (уходят в список исключений сторожа, не чинятся).

Остаток по карте бэклога:

| группа | эпик | задач | что внутри |
|---|---|---|---|
| экраны и их распределение | ANO-128 | 12 | восемь подписей не влезают в телефон (ANO-120); главное число читается как приговор (ANO-76 — провал A1); расшифровка читается как формула (ANO-77 — провал A4); дубли, подписи, слои, журнал без поиска |
| кармашек, примерка, хотелки | ANO-8 | 17 | рамки и расшифровка кармашка, подпись горизонта, тап по дню, ползунки и график примерки, две кнопки «зафиксировать», диалог конверсии, приёмки ANO-48 и ANO-49 |
| первый вход и обещание | ANO-75 | 4 | демо или свои данные, пустые состояния, подача ре-якоря, формулировка обещания |
| правила продукта | ANO-133 | 4 | ANO-63, 101, 123, 124 |
| техническое | ANO-5 | 4 | единая семантика траекторий (ANO-23), скорость примерки и «Стратегии» (ANO-111, 117), окно «Аналитики» (ANO-127) |

**Сверх карты** — заведённое после 12.09 и по смыслу относящееся к «б»: ANO-161, 162, 164, 165, 170, 172, форма тестов ANO-166…168.

**Хвост шага «а»** — «а» закрыт по взятому списку, а не по карте: несрочные денежные задачи ANO-25, 40, 47, 88, 89, 93, 104, 106, 108, 115, 140, 142 и заведённая позже ANO-163.

Всего около семидесяти задач.

## 6. Где у «б» конец — принято владельцем 23.09.2026

Закрыть «б» списком — это около семидесяти задач, то есть несколько спринтов, и часть из них к показу отношения не имеет: «календарь как основной вид» (ANO-65), архитектурная правка траекторий (ANO-23).

Предлагается черта по смыслу, а не по списку: **«б» закончен, когда посторонний проходит этап A** чек-листа `2026-08-30-manual-test-checklist.md`:

* **A1** — ответ читается без объяснений, за пять минут, без подсказок;
* **A2** — ответ есть на неполных и устаревших данных;
* **A3** — ре-якорь ощущается как «несколько цифр раз в месяц»;
* **A4** — объяснение числа читается предложениями, а не формулой.

5 сентября этап провалился на A1 и A4. С тех пор дашборд перестроен (ANO-119), так что A1, возможно, уже проходит — это замер, а не догадка, и стоит он один сеанс с посторонним.

| вариант | что значит | цена |
|---|---|---|
| по списку | закрыть все ~70 | несколько спринтов; «в» и живые люди сдвигаются на столько же |
| по черте | закрыть то, что мешает этапу A, плюс открытые High; остальное — в «д» | черту проверяет посторонний, а не владелец, — нужен человек на один сеанс |

**Принято по черте.** Владелец 23.09: «Черту ты сам правильно провёл, я согласен».

Следствия: «б» закрывается прогоном этапа A с посторонним, а не числом закрытых задач. Задачи «б», которые не мешают этапу A и не несут High, уходят в «д» — разбор, какие именно, делается перед прогоном, по результатам первого сеанса. Первый сеанс нужен и как замер: после ANO-119 A1 мог уже пройти. Задача — ANO-179.

## 7. Как проверяется готовность задачи в этом спринте

Цикл прежний: разбор inline → спека (с планом в одном документе, если шагов немного) → TDD → мутации → стенд → PR → Linear.

Новое с этого спринта, по разделу 9 ретро:

* локально гоняется только затронутое; полный набор — дело CI;
* готовый дифф — сигнал владельцу на `/code-review`;
* для CI мутационная проверка означает: каждая проверка хотя бы раз показана красной на подсаженной поломке.

## 8. Ход спринта — на 25.09.2026, вечер

| # | задача | итог |
|---|---|---|
| 0 | гигиена | сделано 23.09 |
| 1 | ANO-26 CI на PR | #51, Done |
| 2 | ANO-175 сторож словаря | #53, Done |
| 3 | ANO-173 характер строки | #55, Done: «Бронь · Ожидание · Хотелка», поле «Характер» |
| 4 | ANO-176 журнал на два списка | #56, Done. Ревью Codex дало два P1 про двойную запись факта — исправлены |
| 5 | ANO-177 целостность идеи | #57 и #58 после разбора с владельцем, Done. Документ живой: `2026-09-24-idea-integrity.md` |
| 6 | ANO-143 способ входа | отложено до черты. Развилка из задачи устарела: 406-ФЗ запрещает вход через Google и Telegram. Владелец согласен на Яндекс ID + VK ID; как делать — Keycloak или напрямую — решим у черты |
| резерв | ANO-172 план-факт на «Аналитике» | #59, Done |
| резерв | ANO-163 привязка копилки и прошлый капитал | #61, Done: история привязок `fund_account_links`, прошлое больше не переписывается ни привязкой, ни отвязкой |
| «б» | ANO-124 «траектория», «буфер» | #62, Done: «остаток» и «подушка»; канон правила 13 поправлен — «бегущий остаток» оказался калькой |
| «б» | ANO-185 строка «нет ожиданий» | #63, Done. Ревью Codex дало P2: переводы в копилку и синтетические взносы прятали строку — исправлено |
| «б» | ANO-100 с ANO-76 режим нехватки | #65, Done 25.09 после «ок» владельца. Вариант Б «сначала дата»: янтарная карточка, «Сегодня по плану не хватает N», закроет ли доход, что можно сдвинуть; «Кассовый разрыв!» молчит о том же разрыве. Спека — `2026-09-24-gap-mode-design.md`. **ANO-76 остаётся открытой:** Linear закрыл её сам по номеру в заголовке PR — возвращена в работу |
| «б» | ANO-92 НЗ кармашка | #67, Done. Три решения владельца: имя «НЗ» — порог «Хотелок» остаётся «подушкой»; поле внизу «почему столько» и в «Настройках»; режим «НЗ задет» по образцу нехватки. Форма ключа настроек решена ещё 11.09 в ANO-146. Спека — `2026-09-25-nz-design.md` |
| «б» | ANO-186 справка в продукте | #68, Done. Требование владельца 25.09: всё полезное для понимания — в справке прямо в продукте. Вариант А — «?» на экранах, тексты в `lib/help.ts`. Первые: «Как считается кармашек» (с методом 6.1) и «Как устроен журнал» (2.5); «Что такое капитал» переехал. Спека — `2026-09-25-help-design.md` |
| «б» | ANO-77 с ANO-101 расшифровка «почему столько» | **PR #70 ждёт ревью владельца**; CI зелёный, Codex замечаний не дал. Вариант В по макету: число и пояснение, итог по состоянию карточки под чертой, оговорки отдельно; «если погасить карты до планки». Спека — `2026-09-25-breakdown-readable-design.md` |

**Категории** велись владельцем в отдельном чате под ANO-151 и вернулись в спринт итогом:

* итоговый список v4, правило создания категории и рамка трёх вопросов — `2026-09-18-journal-categories-requirements.md`, §4.6 (коммит `0be7930`);
* «Долги и переводы» заменены флагом «жду возврат» — ANO-181;
* правило создания показывается на экране создания — ANO-180 (коммит `d74adba`);
* найдено здесь: ANO-178 — пустой выбор категории в форме правки; проявится при переразметке под v4;
* сама ANO-151 — шаблон категорий при регистрации — открыта: это шаг «в».

**Сверх плана заведено:**

* ANO-184 — знак у факта в журнале;
* ANO-185, ANO-186, ANO-187 — из разбора ANO-177;
* ANO-100 взята в «б», приоритет High;
* ANO-188 — платёж по кредиту из хотелки заводится как «Ожидание», и пропущенный платёж кармашек не держит; найдено ревью ANO-185, эпик целостности денег;
* ANO-92 взята в «б» по правилу черты (открытая High): подушку кармашка на экране не задать — фронт не зовёт настройку. Владелец согласен: после ANO-100.
* ANO-189 — справка на остальных экранах: «Хотелки», «Цели», «Стратегия», «Аналитика». Хвост ANO-186; место в очереди — решение владельца.

**Способ работы, поправлен 24.09.**

* Сам вливаю только механическое — CI, сторожа, тесты, доки без выводов — после ответов Codex.
* Вывод или продуктовое решение сначала разбирается с владельцем в чате, по вопросу за раз.
* Готовый дифф — сигнал владельцу; вливание — после его «ок».
* Задача не закрывается, пока владелец не видел вывод.
* Макет варианта — файлом, артефактом или снимком стенда. Встроенные виджеты в чате у владельца не отрисовываются: 24.09 он выбирал вариант ANO-100 по описанию.
* Снимок стенда для владельца — безголовым Edge (`msedge --headless=new --screenshot --window-size=500,760 --virtual-time-budget=8000`); уже 500 px он не раскладывает. Когда нужны нажатия и прокрутка — тот же Edge через CDP (`--remote-debugging-port`), Node 22 умеет WebSocket без пакетов.
* **Объяснения — в справку продукта** (владелец 25.09). Новое понятие или режим на экране — сразу вопрос, где человек прочтёт, что это; текст справки входит в ту же задачу.
* **Номер задачи в заголовке PR закрывает её при вливании.** В заголовок — только задачи, которые этот PR закрывает; остальные — в описание.

#56 и #57 были влиты без этого — ошибка; ANO-177 разобрана с владельцем задним числом.

## 9. Куда легли новые задачи — без сеанса с посторонним

Первый сеанс с посторонним должен был рассортировать остаток «б» (раздел 6). Человека нет. Поэтому владелец 24.09 принял правило: **что дёшево и лежит на пути постороннего по первому экрану — в «б»; остальное — после черты.**

| задача | куда | почему |
|---|---|---|
| ANO-100 режим ниже границы | «б» | мешает A1; High |
| ANO-185 строка «нет ожиданий» | «б» | дёшево; A2 — ответ на неполных данных |
| ANO-186 тексты метода (6.1) | «б» | только тексты; первое, что встретит новый человек |
| ANO-124 «траектория», «буфер» | «б» | полчаса работы; слова на главном экране |
| ANO-178 пустая категория в форме | до того, как владелец переразметит данные под v4 | иначе пустые поля всплывут ровно тогда |
| ANO-151 шаблон категорий | «в» | нужен вход |
| ANO-180, ANO-181, ANO-187 | после черты | этапу A не мешают |
| ANO-78 живые пользователи | «г» | нужен сервер |
| ANO-189 справка остальных экранов | место — решение владельца | не первый экран постороннего |

**Решение владельца 25.09, вечер: «к бэку пока что».** После ANO-77 очередь «б» по первому экрану предлагала ANO-120 (навигация на телефоне), остаток ANO-76 (слово «кармашек»), ANO-123 (упрёки) и ANO-99 (фраза повторяет число). Владелец: ANO-120 «точно мимо», остальные «окей», но сейчас — бэк. Взяты пять задач эпика «целостность денег» (ANO-132), порядок — раздел 10.

**Открытый вопрос — сама черта.** «Б» кончается, когда посторонний проходит этап A, а человека нет. Сейчас это ничего не блокирует: задачи «б» делаются. Но к концу «б» надо решить, чем проверять черту.

## 10. Точка входа для следующей сессии

1. **PR #70 (ANO-77, ANO-101) ждёт «ок» владельца.** CI зелёный, Codex дал 👍 без замечаний, снимки у владельца. После «ок» — влить; обе задачи закроются сами по номерам в заголовке, дописать закрывающие комментарии.
2. **Бэк, эпик «целостность денег» — пять задач, решение владельца 25.09.** По порядку:
   1. **ANO-188** — платёж по кредиту, заведённый из хотелки, получает характер «Ожидание»; прошла дата без факта — кармашек его не держит, главное число завышено. Платёж по кредиту — бронь: сумма и дата известны заранее. Для уже заведённых платежей, вероятно, миграция — метка «меняет-данные».
   2. **ANO-108** — «Стратегия» считает зафиксированную хотелку дважды, а необсуждённую считает вообще.
   3. **ANO-140** — прогноз кладёт месячную сумму в окно горизонта целиком (`PocketEngine.buildForecastByDay` → `spreadEvenly`); жив по коду 25.09. Искажает «с обычными тратами» и мягкое предупреждение; сейчас не виден — прогноз выключен у всех категорий (V21).
   4. **ANO-88** — «Пополнить фонд» предлагает вдвое больше кармашка: `FundPlannerService` вычитает только брони.
   5. **ANO-85** — API отдаёт 500 там, где должно быть 400 или 404.
3. **Отложено владельцем:** ANO-120 («точно мимо»). Остаток ANO-76 (слово «кармашек»), ANO-123 (упрёки; список от 07.09 перемерить — часть сняли ANO-119 и ANO-172), ANO-99 — «окей», после бэка.
4. **ANO-76 открыта.** Linear закрыл её при вливании #65, возвращена 25.09. Открыты: слово «кармашек» в обычном режиме, проверка на постороннем (ANO-179).

ANO-178 — вне очереди, как только владелец сядет переразмечать данные под категории v4. Открытый вопрос раздела 9 — чем проверять черту без постороннего — к концу «б».

**Способ работы** — раздел 8. Передача между сессиями — через этот раздел, промт не нужен.
