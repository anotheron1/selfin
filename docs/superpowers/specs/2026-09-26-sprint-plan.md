# Спринт с 26.09.2026: правки до входа

Состав и порядок вех — решения владельца 26.09, ретро `2026-09-26-sprint-retro.md`, раздел 6. Порядок задач внутри спринта — по зависимостям, мой. Где в нём решение, а не зависимость, — сказано прямо. Точка входа для сессий этого спринта — раздел 9.

---

## 1. Где мы на маршруте

```
1. Правки до входа   ← этот спринт        веха 1, 27 задач
2. Вход                                   веха 2, 13 задач — эпик ANO-135
3. Выкладка                               веха 3, 7 задач — эпик ANO-136
4. Дизайн                                 веха 4, 34 задачи — ANO-128
5. После запуска                          веха 5, 31 задача
```

Владелец 26.09:

> насчёт порядка — нет, дизайн не торопит, сначала правки, все 25. то есть варик А
> и после правок делаем аутентификацию/авторизацию. потому что сразу при выкладке начнут пользоваться 4 человека

Из 25 правок разбора две — приёмки ANO-48 и ANO-49 — ушли в «4. Дизайн»: часть починок, которые они принимают, — дизайн-задачи. Остаётся 23. Вместе с воротами ANO-194 и тремя задачами с паузы — 27.

## 2. Цель

**Продукт не теряет введённое, не выдумывает лишнего и не показывает неверных чисел — до того, как в нём появятся деньги четырёх человек.** Каждый PR проходит механические ворота, а не память.

Готово, когда:

* ворота ANO-194 работают на каждом PR;
* три задачи с паузы воспроизведены — починены или закрыты замером;
* 23 правки закрыты или перенесены с причиной и «ок» владельца.

## 3. Задачи — порядок по зависимостям

### 0. Ворота — ANO-194, первыми

Все остальные PR спринта пойдут через них. Четыре PR:

| PR | ворота | что | красное на подсаженной поломке |
|---|---|---|---|
| 1 | 1 и 2 | `tools/pr-ready.sh <N>`; CI-джоба «замечание Codex без ответа»; шаблон PR | замечание без ответа |
| 2 | 3 | проверка спек в CI: раздел «Правила продукта — сверка» в изменённой `*-design.md`; «Как мерил» — в спеке с перемером | спека без раздела |
| 3 | 4 | коммит сборки: аргумент образа `GIT_COMMIT`, ручка `GET /api/v1/version`, тест на неё | ручка без аргумента |
| 4 | 5 | сторож: каждый `*RegressionTest` объясняет, почему его числа верны. Дополнить два существующих — `PocketMigrationRegressionTest` и `PocketInputAssemblerAnchorRegressionTest` | эталон без объяснения |

**Проверить на первом PR:** встаёт ли джоба, запущенная событием комментария (`pull_request_review_comment`, `issue_comment`), в проверки PR. У таких событий свой `GITHUB_SHA`, не голова ветки. Если не встаёт — остаётся скрипт, и вливание только через него.

Сделать джобу обязательной в правилах ветки — настройка репозитория, решает владелец.

PR ворот механические, вывода в них нет: вливаю сам после CI и Codex.

### 1. С паузы — воспроизвести на стенде одним заходом

| задача | что | если воспроизведётся |
|---|---|---|
| ANO-169 | быстрый ввод «В копилку» не переводит деньги и теряет сумму | блок A, первой |
| ANO-171 | с экрана «Хотелки» копилку не пополнить | блок B, если дело в логике; «4. Дизайн», если не хватает только кнопки |
| ANO-174 | на «Целях» кнопка пополнения исчезает молча | так же |

Не воспроизвелась — закрыть с замером в комментарии: что делал, на какой сборке (проба `/api/v1/version`), что увидел.

### A. Продукт теряет или выдумывает введённое — 7 задач

| # | задача | что | почему здесь |
|---|---|---|---|
| A1 | ANO-192 | факт не идемпотентен на сервере: повтор запроса даёт второй факт | в Linear Low; ставлю первой — это решение, а не зависимость. Запись факта — главное действие, а на телефоне в мобильной сети ответ теряется чаще. Кнопка держит занятость до перечитанного журнала (ANO-176), но от потерянного ответа не спасает: сервер записал, телефон ответа не получил, человек жмёт снова |
| A2 | ANO-141 | диалог конверсии предлагает то, что бэк отвергнет, и глотает ошибку | High. A2–A4 — вокруг конверсии хотелки: диалог, журнал после неё, служба конверсии |
| A3 | ANO-106 | после конверсии в журнале две неотличимые копии | High. Числа верны, врёт экран: в `FinancialEventDto` нет статуса хотелки, фронту нечем отличить строки |
| A4 | ANO-104 | «копить по 30 000 в месяц» молча не создаётся | `WishlistConversionService` читает `createRecurringPayments` только для кредита |
| A5 | ANO-107 | «Отложить» — дорога в один конец | High. A5 и A6 — переходы статуса хотелки |
| A6 | ANO-183 | смена характера прячет хотелку, статус не вернуть | |
| A7 | ANO-162 | запись из «Что с капиталом» стирает поля, которых не знает форма | |

### B. Путь первого экрана и запреты — 5 задач

| # | задача | что | заметка |
|---|---|---|---|
| B1 | ANO-184 | минус не по смыслу: «--5 000 ₽» у денег из копилки, зарплата со знаком расхода | журнал и «Последний факт» |
| B2 | ANO-170 | быстрый ввод на любую ошибку винит поля | правило 5. После #76 сервер отдаёт на ошибку клиента 400 с причиной — есть что показать |
| B3 | ANO-178 | в форме правки события с удалённой категорией пустой выбор | проявится при смене набора категорий, ANO-151 |
| B4 | ANO-165 | «Структура месяца»: 900000% бюджета и приговоры вместо чисел | запрет 1 и правило 12. Что показывать вместо — вывод, с владельцем |
| B5 | ANO-121 | снимок бюджета пишется, но не читается | убрать или начать читать — решение владельца |

### C. Числа расходятся или молчат — 11 задач

| # | задача | что | заметка |
|---|---|---|---|
| C1 | ANO-89 | инвентаризация: все места, где продукт отвечает «сколько у меня есть». Для каждого — брать из ядра или назвать другим словом | High. Документ без кода — карта для C2–C4. Закрывается, когда каждое место решено |
| C2 | ANO-93 | буфер не доходит до хотелок | по карте C1 |
| C3 | ANO-190 | «Что с капиталом» считает платёж по кредиту дважды | по карте C1 |
| C4 | ANO-191 | Стратегия не видит копилку | по карте C1 |
| C5 | ANO-161 | блок «Хотелки» на «Аналитике» не смотрит на статус хотелки | та же болезнь: экран отбирает хотелки по-своему |
| C6 | ANO-25 | факты, записанные правкой, не попадают в расшифровку Стратегии | C6 и C7 — факты по своему правилу, один разбор |
| C7 | ANO-47 | мостик на «Аналитике» считает факты по своему правилу | |
| C8 | ANO-105 | ползунок «когда» не двигает график | High, фронт |
| C9 | ANO-40 | «Зафиксировать» на кредите не двигает свободные деньги | сначала развилка владельцу: три варианта в задаче |
| C10 | ANO-95 | виновник узкого дня не назван, если у расхода пустое описание | дашборд, объяснение |
| C11 | ANO-164 | привязка копилки к счёту молча меняет «накоплено» | одно предложение в диалогах привязки и отвязки |

## 4. Сознательно не берём

* **Подачу экранов** — заморожена до концепций дизайнера. Если правка упирается в подачу — минимальная, без редизайна.
* **Код входа** — веха 2.
* **ANO-179**, посторонний на этапе A: посторонними станут первые четыре человека после выкладки.
* **ANO-48, ANO-49** — приёмки, после дизайна.
* **Скорость** Стратегии, примерки и журнала — ANO-117, 118, 167, веха 5.

## 5. Параллельно, без кода — у владельца

| что | задача | зачем сейчас |
|---|---|---|
| способ входа: Яндекс ID и VK ID; Keycloak или напрямую | ANO-143 | первая задача вехи 2. Постановка — отдельным сообщением ближе к концу спринта, чтобы вход стартовал без простоя |
| список категорий v4 | ANO-151 | шаблон каждого нового пользователя; владелец ведёт в отдельном чате |
| правовой минимум | ANO-196 | юрист — самый долгий срок на всём пути до выкладки, начать раньше |
| модель угроз | ANO-45 | решить до выкладки; разбор — ретро, раздел 6 |
| хостинг в РФ | ANO-19 | выбор и цена |
| обязательная проверка ворот 1 | — | настройка репозитория |

## 6. Как проверяется готовность задачи

Цикл прежний, этапы не пропускать (владелец 26.09):

разбор inline → спека со сверкой по каждому правилу канона → план → TDD → мутации на итоговом коде → стенд до и после → PR → ворота → «ждёт ок» → вливание → Linear с комментарием.

Новое в этом спринте:

* **Ворота.** После ANO-194 «ждёт ок» и вливание — только при чистом `tools/pr-ready.sh <N>`. До неё — те же проверки руками, по списку ANO-194.
* **Codex** ставит 👀 при открытии PR, минут через десять — замечания или 👍. После пуша правок сам не смотрит: попросить комментарием `@codex review`.
* **Кто вливает.** Вывод или решение — после «ок» владельца. Механическое — тесты, сторожа, документы без выводов — вливаю сам после CI и Codex.
* **Стенд** — пробой `GET /api/v1/version`, не памятью.
* **Деньги**, блоки A и C: интеграционный тест на Testcontainers обязателен. Юниты на моках не видят транзакций.
* **Правка в N местах — N тестов**, у каждого своя мутация.
* **Замер** — через продукт, не через SQL. Поиск — по значению, выдачу не обрезать. `grep -i` здесь не складывает регистр кириллицы.
* **Стопка PR** — перед вливанием нижнего перенацелить верхний на `main` и обновить ветку.
* **Метка «меняет-данные»** — на PR, где миграция намеренно меняет живые данные.

## 7. Где конец спринта

Веха 1 — около двух спринтов по оценке ретро. План держит всю веху. Когда остановиться — решает владелец; остаток переносится в следующий план с тем же порядком.

Следующая веха — «2. Вход». Порядок задач — комментарий к ANO-135 от 26.09 и ретро, раздел 7.

## 8. Ход спринта

| # | задача | итог |
|---|---|---|
| 0 | ANO-194 ворота | — |
| 1 | ANO-169, 171, 174 с паузы | — |
| A1 | ANO-192 | — |
| A2 | ANO-141 | — |
| A3 | ANO-106 | — |
| A4 | ANO-104 | — |
| A5 | ANO-107 | — |
| A6 | ANO-183 | — |
| A7 | ANO-162 | — |
| B1 | ANO-184 | — |
| B2 | ANO-170 | — |
| B3 | ANO-178 | — |
| B4 | ANO-165 | — |
| B5 | ANO-121 | — |
| C1 | ANO-89 | — |
| C2 | ANO-93 | — |
| C3 | ANO-190 | — |
| C4 | ANO-191 | — |
| C5 | ANO-161 | — |
| C6 | ANO-25 | — |
| C7 | ANO-47 | — |
| C8 | ANO-105 | — |
| C9 | ANO-40 | — |
| C10 | ANO-95 | — |
| C11 | ANO-164 | — |

## 9. Точка входа для новой сессии

1. **Прочитать:** этот план, разделы 3 и 6; канон `2026-09-01-product-rules.md`; `CLAUDE.md`.
2. **Состояние на вечер 26.09.** Открытых PR нет: последним влит #84 — ретро и этот план. Стенд собран из `main` 0ff97fd (#80); проба 26.09: плохая дата — 400, итог расшифровки «Свободно», −1 600. Позже влит только #83 — сторож, на стенд не влияет.
3. **Первое действие** — ANO-194, PR 1: скрипт готовности, джоба, шаблон.
4. **Второе** — воспроизвести ANO-169, 171, 174 на стенде.
5. **Дальше** — блоки A, B, C по порядку. После каждого вливания — строка в разделе 8.
