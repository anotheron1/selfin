# ANO-50 — передача в новый чат

Состояние на 5 сентября 2026, конец первой сессии тестирования. Ветка `docs/ano50-testing-plan`.

Читать сверху вниз: первые два раздела нужны, чтобы продолжить, остальное — справка.

---

## 1. Где мы остановились

**Пройдено 5 этапов из 15.** Этап A целиком, этап 0 целиком, этапы 10, 1–2, 6–7, 3–4 первым проходом по API.

| Этап | Состояние |
|---|---|
| **A. Читается ли ответ** | закрыт. Два провала из пяти: A1 и A4 |
| **0. Подготовка** | закрыт. Блок 0.3 признан невыполнимым и удалён |
| **1. Счета и категории** | первый проход по API: 15 сошлось, 0 расхождений. Руками осталось 1.2✓, 1.10✓, 1.13✓, 1.14 |
| **2. Якорь и остаток** | первый проход: сошлось. Руками 2.5✓, 2.8✓ |
| **3. Журнал** | первый проход: 2 расхождения. Руками 3.7, 3.16. Невыполнимы 3.14, 3.15 |
| **4. Повторяющиеся правила** | первый проход: 1 расхождение. Руками 4.9, 4.11. Невыполним 4.10 |
| **5. Кармашек** | **не начат**, 22 блока |
| **6. Копилки** | первый проход: 3 расхождения. Руками 6.1✓, 6.4✓, 6.9✓ |
| **7. Капитал** | первый проход: 7 из 7 без расхождений. Руками 7.7✓, 7.10✓ |
| **8. Хотелки и примерка** | **не начат**, 14 блоков. Там же воспроизведение ANO-48 и ANO-49 |
| **9. Стратегия и аналитика** | **не начат**, 11 блоков |
| **10. Сквозная согласованность** | 10.1 и 10.3 сошлись. 10.4 заблокирован ANO-82. 10.2, 10.5–10.7 не сделаны |
| **11. Объём и отзывчивость** | **не начат**, 5 блоков |
| **12. Настройки и снимки** | **не начат**, 6 блоков |
| **13. Ревизия запрещённого** | **не начат**, 3 блока. Материала уже накоплено много |

Галочка ✓ — блок владелец уже прошёл руками и ответил.

**Ближайший шаг:** этап 5 «Кармашек» (22 блока) либо этап 8 «Хотелки», где ждут два известных симптома ANO-48 и ANO-49.

---

## 2. Как работать дальше

**Протокол изменён по ходу и это сознательно.** Изначально план требовал «один блок в чат». К концу сессии сложилось разделение, которое владелец принял:

* **Claude берёт первым проходом** этапы, где проверка сводится к числам: гоняет сценарии по API, снимает числа, присылает не «всё ок», а список мест, где разъехалось.
* **Владелец проходит руками** только то, что видно на экране: тексты, реакция интерфейса, свободное исследование.
* Отвечает владелец **одним сообщением по всему этапу**, а не блок за блоком.

Владелец прямо сказал: «я немного запутался в поэтапности задач, думал всё просто и по порядку пойдёт». Поэтому **список для него должен быть один, по порядку, сгруппированный по экранам** — чтобы он заходил на каждый экран один раз. Не втягивать его в дорожку Claude, приносить только результат.

**Ограничение первого прохода, о котором нельзя забывать.** ANO-50 заведён потому, что «тесты проверяют расчёты, а пути пользователя — нет». Прогон по API воспроизводит ровно эту дыру. Числовые дефекты он находит (ANO-82, ANO-86, ANO-91 найдены так), а «не понял, куда нажать» и «галочка не снимается» — никогда.

**Правило «не чинить по ходу» действует.** Находки копятся, этап закрывается целиком, потом чинится. Исключение сделано один раз, осознанно: ANO-80 как блокер тестового запуска.

**Три исхода у находки:** живой дефект / шрам старых данных / неверная память о задумке. Третий у автора самый частый и тоже результат.

---

## 3. Стенд

```bash
COMPOSE_PROJECT_NAME=selfin-test docker compose -f docker-compose.yml -f docker-compose.test.yml start
```

Фронт `http://localhost:5174`, API `http://localhost:8081`, БД `localhost:5433`.

**Боевая база не участвует и не участвовала.** Контейнер `selfin-db` остановлен 21 августа, том отдельный (`selfin_postgres_data` против `selfin-test_postgres_data`). Все команды бьют по `selfin-test-db` по имени контейнера.

**Контейнеры падают сами** примерно раз в сутки — просто поднимать заново той же командой.

**Пересоздание базы** (нужно перед восстановлением дампа):

```bash
COMPOSE_PROJECT_NAME=selfin-test docker compose -f docker-compose.yml -f docker-compose.test.yml stop backend && docker exec selfin-test-db psql -U admin -d postgres -c "DROP DATABASE selfin" && docker exec selfin-test-db psql -U admin -d postgres -c "CREATE DATABASE selfin OWNER admin" && docker exec -i selfin-test-db psql -U admin -d selfin -q < ДАМП && COMPOSE_PROJECT_NAME=selfin-test docker compose -f docker-compose.yml -f docker-compose.test.yml start backend
```

**Образ бэкенда пересобирать** после правок кода, иначе стенд гоняет старое:

```bash
COMPOSE_PROJECT_NAME=selfin-test docker compose -f docker-compose.yml -f docker-compose.test.yml build backend
COMPOSE_PROJECT_NAME=selfin-test docker compose -f docker-compose.yml -f docker-compose.test.yml up -d backend
```

`start` образ не подхватывает, нужен именно `up -d`.

### Дампы, `C:\Users\Kirill\selfin-backups\`

| Файл | Что это |
|---|---|
| `selfin-2026-08-21.sql` | исходная боевая копия |
| `ano50-pre-A-2026-09-02.sql` | до этапа A, нетронутая |
| `ano50-after-A3-2026-09-05.sql` | после ре-якоря блока A3 |
| `ano50-etalon-2026-09-05.sql` | **эталонный слепок, блок 0.6** |
| `ano50-before-stage12-2026-09-05.sql` | до этапов 1–2 |
| `ano50-before-stage67-2026-09-05.sql` | до этапов 6–7 |
| `ano50-before-stage34-2026-09-05.sql` | до этапов 3–4 |

### Текущее состояние стенда

Кармашек 26 500, остаток 60 000, якорь **29 августа**. Шесть живых счетов: Основная карта (дефолт), Эталон (наличные, 5 000), Вклад сбер (150 000), ЛукПэй и Бензин (без слежения), Платинум, СберФикс, Тинькофф (кредитки).

Накопленное за сессию: вклад, счёт «Эталон», счета «Бензин» и «Тинькофф», категория «Бассейн». Осталось 6 событий с меткой ANO-50, которые не удалились (409 «Cannot delete PLAN with linked FACTs») — либо добить руками, либо восстановить из дампа.

---

## 4. Инструменты

Всё в `tools/`, всё на Node без зависимостей, всё коммичено.

```bash
node tools/ano50-invariants.mjs --json прогон.json --baseline прошлый.json
```

**Автомат инвариантов.** 20 проверок по семи скоупам, только GET. Гоняется после каждого блока — это шаг 4 протокола. Умеет сравнивать прогон с прошлым: так обнаружилось, что V21 переносит минимум траектории с конца горизонта на реальное событие.

```bash
node tools/ano50-stage10.mjs [--mutate]
node tools/ano50-stage12.mjs
node tools/ano50-stage67.mjs
node tools/ano50-stage34.mjs
```

Первые проходы по этапам. Каждый помечает блоки как СОШЛОСЬ / РАСХОЖДЕНИЕ / СМОТРЕТЬ / РУКАМИ / НЕЛЬЗЯ и не притворяется, что закрыл то, что видно только на экране.

**Грабли, стоившие времени:**

* `Idempotency-Key` на `POST /events` обязан быть **UUID**, иначе 500. На `POST /events/facts` — любая строка.
* `recurring.startDate` **обязателен**, хотя в DTO не помечен `@NotNull`.
* Факт создаётся не через `POST /events` (там только план), а через `POST /events/facts` или `POST /events/{planId}/facts`.
* **Кириллица в `curl` из bash бьётся** (`Invalid UTF-8 middle byte`). Слать JSON через `node --eval` с `fetch`.
* Пути `/tmp` в bash и в node — разные. Использовать каталог scratchpad.

---

## 5. Что найдено. 16 задач, ANO-76…ANO-91

### Срочные

| | |
|---|---|
| **ANO-80** | Первая трата нового пользователя обваливает кармашек в шесть раз. **ПОЧИНЕНО**, миграция V21 |
| **ANO-82** | Факты в день ре-якоря невидимы навсегда; окно `(датаЯкоря, сегодня]` в день ре-якоря пусто целиком; факт на будущий план уводит расход целиком |
| **ANO-86** | Удаление копилки с деньгами: сумма исчезает из свободных и остаётся раздувать капитал |
| **ANO-87** | Перевод в копилку необратим и не ограничен остатком — прошёл перевод 99 999 999 при остатке 27 500 |
| **ANO-91** | Факт на повторяющееся событие падает с 500: аренду и подписки нельзя отметить оплаченными |

### Высокие

**ANO-76** — A1 провален: кармашек читается как приговор прошлому. **ANO-77** — A4 провален: строки-итоги неотличимы от слагаемых, три термина из шести не читаются. **ANO-78** — тестовый запуск. **ANO-83** — блок 0.3 невыполним: транзакции не привязываются к счетам. **ANO-88** — «Пополнить фонд» предлагает вдвое больше кармашка. **ANO-89** — три экрана про одно и то же считают по-разному.

### Средние

**ANO-79** — ре-якорь молча снимает резерв просрочки. **ANO-81** — рамки кармашка не читаются. **ANO-84** — пустой год на графике капитала, копейки съедаются экраном. **ANO-85** — API отдаёт 500 вместо 400 и 404. **ANO-90** — одна подсказка на два графика, нет рекомендуемого взноса.

### Одна общая болезнь

Четыре находки — ANO-79, ANO-82, ANO-86, ANO-91 — про одно: **продукт молча отбрасывает то, что человек ввёл, и никогда об этом не говорит.** Разбивка объясняет состояние и не объясняет, что она проигнорировала.

Вторая общая болезнь — ANO-88 и ANO-89: **у вопроса «сколько у меня свободно» четыре разных ответа** (26 500 / 52 000 / 65 000 / 215 000), два из них на одном экране под одним словом.

---

## 6. Что уже сделано с кодом

Одна правка продукта, осознанное исключение из «не чинить по ходу»:

* `V21__forecast_disabled_by_default.sql` — прогноз незапланированных больше не включается сам. V15 включала его семи засеянным категориям, и первая трата нового пользователя раздувалась до месячной нормы.
* `ForecastDisabledByDefaultIT` — два теста, проверены мутацией.
* Прогон: 339 юнитов + 111 интеграционных, ноль падений.

Коммиты: `67fc923` правка, `4b4f9e1` оснастка и блок 0.1, `d983e3d` этап 10, `1951595` этапы 1–2, `e5de9ea` этапы 6–7, `b848f06` этапы 3–4.

---

## 7. Поправки к самому плану

Найдены по ходу, внесены в `2026-08-22-ano50-testing-plan.md`:

* **Порядок этапа A не по номерам:** A1 → A4 → A5 → A3 → A2. A2 требует чистого стенда и уничтожает данные, на которых стоит A5.
* **Блок 0.3 удалён.** Бумажный эталон на изолированном счёте невозможен: у события нет счёта. Оракул сохраняется двумя другими путями — маленький чистый набор (как в A2) и дельты вместо абсолютов (блок 5.17).
* **Блок 2.4** описан верно, но ожидание требует сначала удалить более свежие якоря.
* **Блок 5.4** требует скоуп «12 месяцев», которого в интерфейсе нет; снимать надо две даты, в текущем месяце и в следующем.
* **Блоки 3.14, 3.15, 4.10** невыполнимы: первые два по ANO-83, третий потому, что `RecurringFrequency = {MONTHLY, YEARLY}` и ритм «5-го и 20-го» одним правилом не выражается.

---

## 8. Документы

| Файл | Что |
|---|---|
| `2026-08-22-ano50-testing-plan.md` | сам план, 15 этапов, с внесёнными поправками |
| `2026-09-03-ano50-block-0-1-findings.md` | 52 расхождения спек с кодом, 143 сверенных утверждения |
| `2026-08-30-manual-test-checklist.md` | источник этапа A |
| `2026-09-01-product-rules.md` | критерии приёмки этапа 13 |
| Страница с фильтрами по блоку 0.1 | https://claude.ai/code/artifact/33cd2ee7-ed82-4959-9e6d-9a6d99e12dec |

**Спека хотелок** `2026-05-29-wishlist-planning-design.md` помечена как частично вытесненная: её раздел «Frontend и UX» описывает экран, которого больше нет. Сверяться с ней постранично нельзя.

---

## 9. Открытые решения владельца

* **Тестовый запуск планируется «в скором времени»** (ANO-78). Пять срочных находок его прямо затрагивают: ANO-82, ANO-86, ANO-87, ANO-91 — все про то, что введённое пропадает. Стоит решить, чинить ли их до запуска.
* **Предикт пересматривается отдельно, после запуска.** V21 его выключила, механизм не трогали.
* **ANO-89** владелец сформулировал сам: «дизайн можно отложить, но расчёты наверняка разные везде». Вторая половина подтверждена числами.
* **Слово «Счета»** — открытый вопрос с августа, ждёт блока 13.2.
* Сроков нет: «не буду ставить ограничение в день, управимся как управимся».
