-- МУТАЦИЯ ДЛЯ ПРОВЕРКИ CI (ANO-26). Не вливать: черновой PR закрывается без слияния.
-- На пустой базе интеграционных тестов проходит; на заполненной обязана упасть.
DO $$
BEGIN
  IF EXISTS (SELECT 1 FROM target_funds) THEN
    RAISE EXCEPTION 'подсаженная поломка: миграция падает на заполненной базе';
  END IF;
END $$;
