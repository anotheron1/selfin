// МУТАЦИЯ ДЛЯ ПРОВЕРКИ CI (ANO-26). Не вливать: черновой PR закрывается без слияния.
import { describe, expect, it } from 'vitest';

describe('подсаженная поломка фронта', () => {
  it('обязана покраснить проверку «Фронт»', () => {
    expect(1).toBe(2);
  });
});
