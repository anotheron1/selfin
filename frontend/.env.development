# URL backend API для локальной разработки (vite dev server).
# В Docker-деплое этот файл НЕ используется — BASE_URL падает на /api/v1,
# а Nginx проксирует /api/ → backend:8080.
VITE_API_URL=http://localhost:8080/api/v1
