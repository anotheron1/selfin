#!/usr/bin/env bash
# Готовность PR к «ждёт ок» и вливанию — ворота 1 (ANO-194).
# Логика — в pr-ready.mjs; этот файл держит имя, под которым скрипт записан в плане спринта.
exec node "$(dirname "$0")/pr-ready.mjs" "$@"
